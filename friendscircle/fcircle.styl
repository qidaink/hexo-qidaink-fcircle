/*====================================================================================*
* FileName: fcircle.styl
* FileInstructions: 站点友链朋友圈自定义样式文件
* Author: qidaink
*=====================================================================================*/
.article-sort
  margin-left: .5rem;
  padding-left: 1rem;
  border-left: 2px solid lighten(#49B1F5, 20);

  &-title
    position: relative;
    margin-left: .5rem;
    padding-bottom: 1rem;
    padding-left: 1rem;
    font-size: 1.72em;

    &:hover
      &:before
        border-color: #FF7242;

    &:before
      position: absolute;
      top: calc(((100% - 1.9rem) / 2));
      left: -.45rem;
      z-index: 1;
      width: w = .5rem;
      height: h = w;
      border: .5 * w solid #49B1F5;
      border-radius: w;
      background: var(--card-bg);
      content: '';
      line-height: h;
      transition: all .2s ease-in-out;

    &:after
      position: absolute;
      bottom: 0;
      left: 0;
      z-index: 0;
      width: .1rem;
      height: 1.5em;
      background: lighten(#49B1F5, 20);
      content: '';

  &-item
    position: relative;
    display: flex;
    align-items: center;
    margin: 0 0 1rem .5rem;
    transition: all .2s ease-in-out;

    &:hover
      &:before
        border-color: #FF7242;

    &:before
      $w = .3rem;
      position: absolute;
      left: calc(-0.9rem - 17px);
      width: w = $w;
      height: h = w;
      border: .5 * w solid #49B1F5;
      border-radius: w;
      background: var(--card-bg);
      content: '';
      transition: all .2s ease-in-out;

    &.no-article-cover
      height: 80px;

      .article-sort-item-info
        padding: 0;

    &.year
      font-size: 1.43em;

      &:hover
        &:before
          border-color: #49B1F5;

      &:before
        border-color: #FF7242;

    &-time
      color: #858585;
      font-size: 95%;

      time
        padding-left: .3rem;
        cursor: default;

    &-title
      display: -webkit-box;
      overflow: hidden;
      -webkit-box-orient: vertical;
      color: var(--font-color);
      font-size: 1.1em;
      transition: all .3s;
      -webkit-line-clamp: 2;

      &:hover
        color: #49B1F5;
        transform: translateX(10px);

    &-img
      overflow: hidden;
      width: 80px;
      height: 80px;

      img
        width: 100%;
        height: 100%;
        transition: all .6s;
        object-fit: cover;

        &:hover
          transform: scale(1.1);

    &-info
      flex: 1;
      padding: 0 .8rem;
      a
        color:#0593d3;
        border-bottom: none;
        &:hover                            /* 鼠标悬停效果 */
          color: #fc6423;
          border-bottom: none;
